{
    "name": "eventscroller",
    "version": "1.0.0",
    "main": "",
    "scripts": {
        "install-client": "cd client && npm install && npm run build && cd ..",
        "heroku-postbuild": "npm run install-client"
    }
}